function calcular() {
  // Pegue o valor do input #txtnumero e converta para Number
  // Valide se o campo está preenchido e maior que zero
  // Crie uma variável soma = 0 e i = 1
  // Use um while (i <= numero) para somar todos os números de 1 até o digitado
  //   Some i à soma a cada repetição
  //   Incremente i com i++
  // Mostre no div #resultado o total
  // Exemplo: somar de 1 até 5 → "1 + 2 + 3 + 4 + 5 = 15"
}
