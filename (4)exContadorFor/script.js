function contar() {
  // Pegue os valores dos inputs #txtinicio e #txtfim e converta para Number
  // Valide se os campos estão preenchidos
  // Valide se o início é menor que o fim
  // Use um for para contar do início até o fim
  // A cada número, adicione no div #resultado com innerHTML +=
  // Exemplo de resultado: "1, 2, 3, 4, 5"
  // Dica: para não colocar vírgula no último número,
  //   verifique se é o último com if (i == fim)
}
