function listar() {
  // Pegue os valores dos inputs #txtde e #txtaté e converta para Number
  // Valide se os campos estão preenchidos
  // Valide se o primeiro valor é menor que o segundo
  // Crie uma variável i = de e um contador total = 0
  // Use um while (i <= até) para percorrer os números
  //   Dentro do while, use % para verificar se i é par
  //   Se for par, adicione no #resultado com innerHTML += e incremente total
  //   Incremente i com i++ ao final de cada repetição
  // Mostre o total de pares encontrados ao final
  // Exemplo: pares de 1 a 10 → "2, 4, 6, 8, 10 | Total: 5"
}
