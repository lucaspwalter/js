function verificar() {
  // Pegue o valor do input #txtnumero
  // Valide se o campo está preenchido
  // Converta para Number
  // Use o operador % (resto da divisão) para saber se é par ou ímpar
  //   numero % 2 === 0 → Par
  //   caso contrário   → Ímpar
  // Caso extra: se o número for 0, mostre "O número é zero"
  // Mostre o resultado no div #resultado
}
