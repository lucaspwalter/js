function somar() {
  // Pegue os valores dos inputs #txtde e #txtaté e converta para Number
  // Valide se os campos estão preenchidos
  // Valide se o primeiro valor é menor ou igual ao segundo
  // Crie uma variável soma = 0 antes do for
  // Use um for do primeiro até o segundo número
  //   A cada iteração, some o valor atual à variável soma
  // Mostre no div #resultado a soma total
  // Exemplo: soma de 1 até 5 → "Soma de 1 até 5 = 15"
}
