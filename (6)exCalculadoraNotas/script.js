function calcular() {
  // Pegue os valores dos 3 inputs e converta para Number
  // Valide se algum campo está vazio
  // Calcule a média: (nota1 + nota2 + nota3) / 3
  // Use if/else para classificar:
  //   média >= 7   → "Aprovado"
  //   média >= 5   → "Recuperação"
  //   média < 5    → "Reprovado"
  // Mostre a média com 1 casa decimal e a situação no div #resultado
}
