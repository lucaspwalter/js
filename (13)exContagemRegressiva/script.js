function contar() {
  // Pegue o valor do input #txtnumero e converta para Number
  // Valide se o campo está preenchido e maior que zero
  // Use um for decrescente: comece no número digitado e vá até 0
  //   Dica: for (var i = numero; i >= 0; i--)
  // A cada iteração, adicione o número no div #resultado com innerHTML +=
  // Ao final, adicione "Fim!" no resultado
  // Exemplo: contagem a partir de 5 → "5, 4, 3, 2, 1, 0 - Fim!"
}
