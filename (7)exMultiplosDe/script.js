function listar() {
  // Pegue os valores dos inputs #txtnumero e #txtaté e converta para Number
  // Valide se os campos estão preenchidos e maiores que zero
  // Use um for de 1 até o valor de "até"
  // Dentro do for, use % para verificar se o número atual é múltiplo de #txtnumero
  //   Se for múltiplo, adicione no div #resultado com innerHTML +=
  // Exemplo: múltiplos de 3 até 20 → "3, 6, 9, 12, 15, 18"
}
